

Bugs:
	- variable stack never uses the first element in StalaJava
	- number parsing can not handle negative numbers


TODO:
	- abstract out jmp, jz, label:
	- function names in comments
	- local variable names in comments
	- full error reporting and positioning
	- escapement of asm string
	- disable comments output for StalaJava


Possible next steps:
	- buffer defines
	- global variables (let outside function)


Optimizations:
	- remove unused functions from final assembly
	- debloat output .asm 


